name: big组件
description: testllla
keywords:
  - 张玉洁的标签
license: MIT123                                   # GPLv2，LGPLv2.1，MIT，Apache license v2.0，BSD
homepage: https://www.taobao.com/
source_file:
  - src/*.c

build_config:
  include:
    - include
  cflag: ""                             # C 编译器所需要要的编译参数
  define:                                      # 宏定义
    - CONFIG_USING_TLS

install:
  - dest: include
    source:
      - "include/librws.h"
